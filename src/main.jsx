import React from 'react';
import ReactDOM from 'react-dom/client';
import SpanCalculator from './SpanCalculator.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SpanCalculator />
  </React.StrictMode>
);
